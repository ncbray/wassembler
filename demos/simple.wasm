func ifelse(cond i32, a i32, b i32) i32 {
  if (cond) {
    return a > b;
  } else {
    return a < b;
  }
}

export func main() i32 {
  var i i32 = 0;
  while (i < 10) {
    i = i + 1;
  }
  return i + ifelse(0, 1, 2) * 2;
}
