export func add(a i64, b i64) i64 {
  return a + b;
}

export func ifelse(cond i32, a i32, b i32) i32 {
  if (cond) {
    return a > b;
  } else {
    return a < b;
  }
}

export func main() i32 {
  return 11 + ifelse(0, 3, 5) * 2;
}
